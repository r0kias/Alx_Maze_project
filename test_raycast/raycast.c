#include <stdio.h>
#include <stdlib.h>
#include <SDL2/SDL.h>

/**
 * endpoint_check - check if the ray of player touch wall
 * @p: player and raycast point
 * @w: wall of map
 * Return: True if p view touch wall, false if not
 */

bool endpoint_check(player p, wall w)
{
	return true;
	return false;
}
